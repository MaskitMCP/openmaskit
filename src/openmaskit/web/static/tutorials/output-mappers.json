{
    "id": "output-mappers",
    "title": "Output Masking with Regex",
    "steps": [
        {
            "text": "Output mappers automatically mask sensitive patterns in tool responses using regex. Example: API keys, IP addresses, email addresses.",
            "target": "section.panel:contains('Output Masking')",
            "position": "bottom"
        },
        {
            "text": "Call your tool in 'Try It Out' first to see a real response. Once you have one, this section will let you add a regex (e.g. '\\b[A-Z0-9]{32}\\b' for 32-char API keys) and preview the matches before saving.",
            "target": ".section-hint:contains('Call the tool')",
            "position": "bottom"
        }
    ]
}
